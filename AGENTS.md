# Aijob 项目协作约束

本文件用于让新的 kiro 任务在缺少历史对话时仍能保持产品意图、工程边界和证据标准。动态进度不写在这里，以 `docs/06-mvp-roadmap.md` 和 `docs/handoffs/current.md` 为准。

## 每个新任务开始时

1. 先读 `README.md`、`docs/06-mvp-roadmap.md`、`docs/handoffs/current.md` 和 `docs/plans/README.md`。
2. 再检查当前分支、`git status` 和最近提交；已有改动属于 coco，不得覆盖。
3. 只执行路线图与交接共同指向的当前目标。不要因为上下文变少而重新定义产品、从头搭架构或提前扩展范围。
4. 如果文档、代码和运行结果冲突，先记录冲突并用可复现检查确认；不要静默选择其中一个。

分支名、提交信息或工程纵向切片名称不代表任何产品 Gate 已通过。`M0–M4` 与 `PA-1` 已完成并归档，`UX-0` 端到端契约审计已关闭；当前交付从 `OS-1` 开始按 `OS-1–OS-7` 前后端同步改进里程碑推进。每个 OS 切片都必须同步记录 Contract、Database/Platform、Web、Integrated Gate 与 Evidence，任何单层通过都不代表切片完成。旧 `UX-1–UX-7` 名称、Phase 2B 嵌套顺序、G4-first 严格总计划、M0–M4/PA-1 计划、归档计划、废止路标、旧验收中的“下一步”和对话摘要均不得覆盖路线图与当前交接。Private Alpha 的供给、安全、服务器和 G0/G1/G4 条件继续由 `docs/plans/private-alpha-readiness-gates.md` 守门，但该清单不提供当前任务。

## 产品使命

Aijob 是“可信官方岗位驱动的完整求职 OS”，不是追求岗位数量的信息流、简历生成器或自动投递工具。它面向近期真实投递实习岗位的中国大陆在校生，以经过准入的企业官网和企业确认的官方 ATS 为岗位事实真源，把资格核对、经历证据、岗位定制简历、投递记录、文字面试和复盘组织在同一个岗位 Case 中，最终仍由用户回到官方页面自行投递。高校就业网站、政府公共就业站、企业认证公众号和其他二手页面只能用于发现企业及其官网方向，不能成为用户岗位目录真源。ADR-0017/0018 将本地假设扩展为中小企业为主的全部职能实习和确定性 JD 洞察；ADR-0030 改变产品组织方式与执行顺序，但不代表价值已经验证，也不取消 100 家企业 / 1000 条岗位的 Private Alpha 供给硬门槛。

首轮成功指标是高质量投递决定，不是抓取数量、页面点击量、匹配分数或 AI 调用量。

## 不可静默改变的产品边界

- 不抓取 BOSS、实习僧、牛客等综合招聘平台。
- 不绕过登录、验证码、访问控制、付费墙或明确禁止的访问政策。
- 技术上可访问不等于获准聚合或公开发布；`pending_review` 来源只能进入本地内部预览。
- 不自动填写、模拟登录、批量投递或替用户提交申请。
- 未在官方页面明确出现的字段必须保留为 `unknown`/“未说明”，不得由规则或模型补写。
- 资格、经历证据和偏好必须分开；不输出匹配度百分比，不自动劝退或隐藏岗位。
- 规则和固定模板必须在 AI 不可用时独立完成资格、匹配和推荐。受控 AI 简历优化是本地完整 MVP 的必需能力，只能由用户显式选择并使用去标识化、已确认的证据；公开或远程环境默认关闭，仍需供应商、合规和增量价值 Gate。
- 简历原文确认后立即删除，最长不超过 24 小时；用户创建或确认的结构化事实、偏好、证据和其他职业资产默认长期保留，并支持单项或全部删除。历史 30 天匿名 owner 机制仅作兼容，迁移顺序以 ADR-0031 为准。

修改以上边界必须有新的用户证据或 ADR，不得在实现中顺手改变。

## MVP 工程边界

- PostgreSQL 是唯一查询和任务真源。
- 保持模块化单体与 `web-api`、`collector-worker`、`match-worker` 三个权限边界。
- 完整本地 MVP 不引入 Redis、独立搜索、向量库、消息总线、Playwright 生产采集或公共管理后台。
- 本地 MVP 允许 PDF/DOCX/纯文本简历输入；文件必须按 ADR-0012 校验并隔离解析，最大 5 MiB，不支持 OCR、图片、旧版 `.doc`、压缩包、宏或加密文件。
- 供给单位是**在校生可投岗位**，不是实习岗位（ADR-0035）：按 ADR-0032 的显式短语逐条判定，排除 `postgrad_only`、`school_restricted` 与明示多年经验者，收入 `reachable` 与 `unknown`。实习、校招、应届生与管培生均纳入；`unknown` 必须收入，把未说明当排除等于用 `unknown` 做否定推断。
- Private Alpha 供给目标为 100 家企业、1000 条可见活动岗位（运营缓冲 110 家、1100 条），人工/浏览器来源不超过企业数 20% 和可见岗位数 10%。这两项按 ADR-0035 是**报告项**：审计必须如实报出且不得省略，但不阻塞任何操作。职能分布与城市分布降为观察指标，不再作为门槛。ADR-0032 的「可达岗位 ≥50%」聚合门槛已撤销——判据下沉到单条岗位后该比例恒真。
- Private Alpha 外部测试前必须覆盖简历解析与确认、三轴匹配、确定性推荐、JD 洞察、AI 对照修改稿、DOCX 导出、官方 URL/企业邮箱交接、五态决定和全部个人数据删除。原 31 条产品/运营目录及任何阶段性目录只作为历史正确性和运行基线，不能代替新范围验收。
- 来源网络目标必须使用精确协议、主机、端口、路径前缀和查询参数白名单；重定向默认拒绝并重新校验。
- 采集任务必须保持幂等、租约、心跳、fencing token、有限重试和失败隔离。
- 岗位修订和匹配运行必须可复现；内容语义未变化时不得伪造新岗位版本。
- 管理与复核能力使用内部 CLI，不为 MVP 创建公共管理界面。

## 证据与质量

- 首次启用、扩大请求范围、恢复暂停来源和浏览器快照必须由人工明确操作；按 ADR-0026 在配置中显式启用的确定性来源可由本机 `collector-worker` 定时刷新。自动化测试、CI、构建、Alpha 和 Production 不访问真实招聘站。
- 来源稳定性按**观察次数与跨度**计量，不依赖在线时长（ADR-0035）：`stableIdentityAndFields` 通过条件为成功刷新 ≥3 次、相邻两次间隔 ≥20 小时、这些刷新之间字段结构未变。来源持续性为至少 3 个已准入确定性来源各累计成功刷新 ≥3 次且跨度 ≥3 天。机器关机只延后达标时间，不产生惩罚。滚动 12 小时全部完成一轮刷新降级为**在线期间**的容量目标，不再作为门槛；ADR-0026 每小时最多 3 个不同来源、24 小时分散排期只是历史灰度实现，ADR-0028 的动态容量只能在检查点通过后受控启用，且不得放宽逐来源网络预算、安全边界或失败隔离。
- 新来源先完成主体证明、访问政策、新鲜度和采集方式评估，再写适配器。收录判据为 ADR-0032 的五项属性：主体可证、内容第一方且完整、投递直达雇主、活跃可核验、可确定性解析刷新。
- **采集方式必须由一次实际尝试决定，不得预判**（ADR-0035）：不登录能看到不等于能确定性获取。确定性抓取成功即进入自动刷新通道；失败则记录原因并转人工快照通道，不是整源否决。需要登录、验证码或绕过访问控制才能看到的来源直接放弃，既不绕过也不转人工通道。
- 投递入口缺失不再阻止采集（ADR-0035）：无投递入口的岗位可入库并进入本机 `local_mvp`，标记为待人工审核；补齐官方投递 URL 或企业域名招聘邮箱后方可对外可见。**对外可见仍然要求投递入口存在。** 岗位正文的职责与任职要求放宽为至少存在其一，两者皆缺不可入库。
- 来源新鲜度只约束对外可见（ADR-0035）：`SOURCE_NOT_FRESH` 与 `JOB_NOT_RECENTLY_VERIFIED` 不阻塞本机 `local_mvp`。新鲜度描述的是来源运行状态，不是岗位数据是否有效。
- 访问政策判据按 ADR-0033：以站点公开 `robots.txt` 与服务条款为依据（取不到 robots 即视为禁止），并承担署名、回链、只保留决策必需原句、删除通道、异议即停与周期复核义务；robots 或 ToS 转为禁止时自动暂停来源。岗位正文只保留职责与任职要求原句，不复制公司简介、宣传与福利文案，且只删减、不改写、不生成。
- 用户可见（Alpha/公开）岗位必须能探知关闭：来源为列表型且消失即关闭，或岗位带明示截止日期；无截止日期且不属任何列表的冻结单条公告不得进入用户可见目录。用户可见岗位「已截止但仍显示」不得超过 7 天。本机 `local_mvp` 内部预览不受此约束，但必须显式暴露 `closure_detectable`。
- 任何来源进入公开查询前必须通过准入；未通过时公开 `/v1/jobs` 保持为空是正确行为。
- `pending_review` 来源可以进入仅限本机的 `local_mvp` 目录，但不得因此标记为获准公开或提升来源稳定性 Gate。
- 不把工程可行性、岗位候选或自动分类结果计为用户价值证据或人工金标。
- 提交前至少运行与改动相称的 lint、类型、测试和构建检查；数据/迁移改动必须增加 PostgreSQL 集成验证。
- 不提交 `.data/`、`.hatch-runs/`、密钥、令牌、简历原文或本地数据库文件。

## 事实源顺序

1. `docs/06-mvp-roadmap.md`：当前产品阶段、Gate、样本进度和下一决定。
2. `docs/handoffs/current.md`：最近工程状态、当前任务和接手检查表。
3. ADR：已批准的架构决定。
4. 其他 PRD、体验、数据、安全和工程文档：稳定规范。
5. 对话摘要或口头描述：仅作为线索，冲突时不得覆盖仓库事实源。
