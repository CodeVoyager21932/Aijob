# G2 SME 批次 07-03 执行记录

- 执行日期：2026-07-31
- 离线留档日期：2026-08-01
- 授权依据：ADR-0022、ADR-0025
- 来源范围：深圳市望尘科技有限公司
- 执行边界：匿名只读、单页低频；未使用登录、验证码、Cookie、CSRF、动态签名、代理、OCR 或浏览器快照
- 时效边界：真实读取、修复后重跑和同小时幂等验证均在 2026-07-31 截止前完成；2026-08-01 起不再访问本批五个已截止页面
- 结论：1/1 来源通过，新增 1 条 `local_mvp` 岗位；公开 `/v1/jobs` 仍为 0

## 1. 候选与契约

本批五家候选的活动性、规模、申请链和访问边界见[候选核验记录](g2-sme-batch-07-03-candidate-assessment-2026-07-31.md)。仅望尘科技在截止当日同时满足接入硬门槛：

| 来源 | 契约 | 规模证据 | 首批上限 |
| --- | --- | --- | --- |
| `galasports-internships` | 一张冻结香港中文大学（深圳）详情页；只读取体育游戏策划实习；申请方式为原文明示的 `huangtingting@galasports.com` | 望尘官网披露团队 700 余人，港交所 2025 年年报披露 591 名全职雇员，均映射为 `medium` | 1 |

SKT 思珂特教育、GURU Game、广州深蓝互动和浙江白鹭资产分别因完整 JD、主体/申请链或合格规模证据未闭合而来源级暂停。望尘来源保持 `pending_review`、`local_mvp` only；`accessPolicyAccepted` 保持失败，不构成公开聚合授权。

## 2. 结构修复

首次运行发现港中深详情页在任职要求后继续读取“招生网”等站点页脚，导致页脚文本混入岗位要求。执行方没有手工修改数据库正文，而是：

1. 将共享 `university-employment-detail-html` 适配器升至 `0.1.3`；
2. 在港中深正文解析中加入“招生网”和版权行的确定性结束标记；
3. 增加望尘正常解析、页脚排除和外域邮箱拒绝测试；
4. 同步提升所有使用共享适配器的来源政策版本；
5. 在截止前以新政策重跑望尘，生成可复现修订。

最终数据库正文不含港中深页脚，职责和任职要求仍完整保留，原文明示的企业域名邮箱没有被截断。

## 3. 真实首批与幂等

| 来源 | 最终 run / task | 政策/适配器 | 请求 | 发现/规范化/拒绝 | 完成态 | 重放 |
| --- | --- | --- | ---: | ---: | --- | --- |
| 望尘科技 | `09c2b783-859b-4893-b157-e8520a85bc4a` / `638d30eb-45b5-44d0-a574-d80db9da942d` | v2 / `0.1.3` | 1 | 1 / 1 / 0 | `partial` | 同小时 `reused=true` |

- 唯一请求严格对应冻结详情页，未遍历高校列表或更多岗位。
- `completion=partial` 按单页来源配置和实际运行结果如实保留，不扩大请求范围补齐数量。
- 同小时重放返回原 `taskId/runId`，未触发新网络请求，也未产生伪岗位修订。
- 截止日后的工作仅限离线核对和留档；不会再次触发该来源请求。

## 4. 物化结果

`pnpm catalog:materialize`：

```text
eligibleRevisions=199
createdVersions=1
createdRequirementSets=1
suspectedDuplicatePairs=0
quotaSelectedJobs=135
quotaSuppressedJobs=64
```

目录实际分母：

| 指标 | 批次前 | 批次后 |
| --- | ---: | ---: |
| 总供给 | 198 | 199 |
| 可见岗位 | 134 | 135 |
| 企业 | 23 | 24 |
| SME 企业 | 7/23（30.43%） | 8/24（33.33%） |
| SME 可见岗位 | 20/134（14.93%） | 21/135（15.56%） |
| 配额压缩 | 64 | 64 |
| 疑似重复 | 0 | 0 |
| 公开岗位 | 0 | 0 |

本批一条岗位进入 `local_mvp` 可见目录；没有修改公共 API、数据库表、配额算法或公开来源准入状态。

## 5. 截止后失效处理

2026-08-01 只进行离线状态收口，没有再次访问高校或企业页面：

1. 将 `galasports-internships` 升为政策 v3 `paused`，并把 `localProbe.enabled` 设为 `false`；
2. `pnpm source:assess galasports-internships` 只登记本地配置，确认 `policyStatus=paused`、`liveProbeEnabled=false`；
3. 再次物化得到 `eligibleRevisions=198`、`createdVersions=0`、`createdRequirementSets=0`、疑似重复 0；
4. 按目录查询的真实过滤条件复核为 198 总供给、134 可见、23 家企业，SME 为 7/23 家和 20/134 岗位，公开岗位 0。

配额选择表为保留不可变已发布历史仍报告 `quotaSelectedJobs=135`；当前目录会按来源当前政策排除望尘，因此不能把该历史配额行误报为当前可见岗位。望尘的截止前修订、岗位版本和合格规模证据保留，但不再计入活动目录或 SME 当前分母。

## 6. 工程门

显式使用隔离数据库：

- `pnpm test`：412/412（platform 319、web 57、config 16、contracts 15、database 5）
- `pnpm typecheck`：通过
- `pnpm build`：通过
- `pnpm lint`：通过，281 文件
- `git diff --check`：通过

## 7. 后续决定

截止当日导入后曾为 199 条总供给、135 条可见岗位、24 家企业；按截止日失效后当前为 198/134/23，SME 为 7/23 家与 20/134 岗位。G2 的 300–500 条可见、30–40 家企业、SME 企业不少于 60% 和 SME 岗位不少于 50% 仍未满足。下一批继续按冻结排序规则选择仍在有效期内的候选；不沿用 2026-07-31 已截止页面的活动性结论，也不降低规模、字段和申请链标准。
