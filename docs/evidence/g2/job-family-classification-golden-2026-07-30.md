# P6 跨职能分类金标（2026-07-30）

## 1. 结果

- 正式金标：`fixtures/gold/job-family-classification.golden.csv`，共 50 条。
- 覆盖全部 12 个 `JobFamily`，每个职能至少 2 条；其中 known 37、conflict 11、unknown 2、需人工复核 19。
- 覆盖纯标题推断、官方类目与标题冲突、产品质检、算法工程、`other`、未知负例及跨职能边界。
- A 标注完成后，由独立会话在不可见 A 结果的条件下完成 B 盲标；A/B 50/50 一致，无遗留分歧。原始 B 输出保存在 Git 忽略的 `.data/job-family-classification-b.csv`，不进入仓库。

## 2. 守门

- `job-family-classification.golden.test.ts` 逐条重放分类器，断言状态、职能集合和 `requiresManualReview`。
- 测试同时断言总量不少于 50、case id 唯一、12 职能每类至少 2 条、A/B 与裁决字段完整。
- 既有三轴工程金标最低守门从 20 固定为 32；当前实际 40 条。

## 3. 分类器修正与目录影响

- 金标发现“内容营销实习生”仅凭标题应归入 `marketing` 并要求人工复核；标题词表补充“营销”。
- 修正后 50 条金标全部通过。既有 177 条目录修订是不可变输入，没有因规则变化回写或伪造版本。
- 2026-07-30 重物化共创建 1 个新版本，该版本来自批次 04 的中信新岗位；分类器词表修正对既有目录创建版本数为 0、配额选择差异为 0。
