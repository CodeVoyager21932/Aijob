# G2 正确性重新验收记录（2026-07-20）

## 1. 结论

`G2 Local Complete MVP` 在 2026-07-20 完成正确性修复并重新通过工程验收。该结论只表示本地完整 MVP 的实现与回归门通过，不表示产品价值、来源公开准入或公开 AI 已验证：产品证据仍为 `E0`，G0/G1 尚未开始，G3 仍为 0/3。

本轮只使用仓库已有离线岗位快照和纯合成简历，不重新访问招聘站。浏览器验收创建的匿名 owner、画像、推荐、优化和 DOCX 已通过产品内“删除全部个人数据”闭环清理。

## 2. 被推翻的旧结论与修复结果

| 旧缺陷 | 重新验收结果 |
|---|---|
| 岗位展示字段与要求集双真源 | 31 个活动岗位都绑定 `deterministic-requirements-v4` 活动要求集和唯一条件投影；列表、详情、筛选、匹配读取同一投影 |
| “优先”整句传播、在读漏解析 | 目标美团岗位将“大学本科及以上学历在读”“实习时间至少3个月”“每周到岗位不少于4天”分别解析为 `required` 原子要求；计算机专业、AI 项目和 SQL 单独为 `preferred` |
| PDF/无换行文本形成整份单证据 | `ResumeDocumentRevision` 保存有序 section/block；1612 字无换行文本拆为 4 个不超过 500 字的块；`ResumeEvidence v2` 只引用单一 `sourceBlockId` |
| 简历事实候选不足或错位 | 学历、专业、毕业年份、在校状态和技能候选均有回归；真实浏览器验收额外发现并修复“信息管理专业”被吞入后续学历文本的问题 |
| “都可以”成为真实城市 | “都可以/不限/无所谓/不限城市”归一为空偏好；与具体城市混用返回 422；旧数据进入匹配前也归一化 |
| 推荐建立在错误粒度并显示 1–31 名次 | 推荐按“未发现明确冲突 / 需补充信息 / 存在明确冲突”分组，组内使用 `decision-readiness-v2`；展示 coverage、basisState 和类型化 gaps，不再展示名次徽章 |
| AI 复制整段 claim、DOCX 压平 | AI 针对单个 `sourceBlockId` 返回真实 `suggestedText`、岗位要求引用和证据引用；未选区块按原章节与顺序保留；DOCX XML 保留真实章节 |
| 岗位详情偶发丢弃当前匹配 | 将“岗位版本仍在加载”与“已确认版本不一致”分开；真实页面加载竞态和单元回归均通过 |
| 320 px 优化页横向溢出 | 最终完整预览改为保留换行并允许长文本折行；修复后 `clientWidth=305`、`scrollWidth=305` |

## 3. 离线目录与数据库断言

执行 `pnpm catalog:materialize`：

```text
eligibleRevisions: 31
createdVersions: 0
createdRequirementSets: 0
suspectedDuplicatePairs: 0
```

PostgreSQL 当前活动目录为腾讯 14、南开就业网·好未来 7、美团 10，共 31 条；31/31 都有活动 v4 要求集与唯一条件投影。物化没有创建新岗位版本，符合“解析器升级只创建新要求集、不伪造官方岗位版本”的约束。

目标美团岗位 `产品经理实习生`（岗位版本 `83a35c6d-a61b-4ca9-b1db-1b4eed927694`）的投影为：

- 学历：本科、硕士、博士，`required`
- 在校状态：`true`，`required`
- 实习时长：至少 3 个月，`required`
- 每周出勤：至少 4 天，`required`
- 计算机相关专业、AI 项目经验、SQL：各自独立，`preferred`

## 4. 自动化工程门

| 检查 | 结果 |
|---|---|
| `pnpm lint` | 202 个文件通过，无修改 |
| `pnpm typecheck` | config、contracts、database、platform、web 全部通过 |
| PostgreSQL 全量测试 | 244 项通过：config 16、contracts 11、database 5、web 52、platform 160 |
| `pnpm build` | 全工作区 TypeScript 与 Vite 生产构建通过 |

回归覆盖包括三种简历输入、PDF/DOCX 安全边界、1612 字单块、专业/学历/毕业年份/在校状态候选、不限城市 422、preferred unknown 不进入硬条件待确认、旧 v1 只读、31 条确定性推荐、逐块建议、未选区块保留、DOCX `word/document.xml` 章节顺序、全部个人数据删除和迟到任务拒绝。

## 5. 真实浏览器验收

使用本机 `web-api`、`match-worker`、PostgreSQL 和正式 `/jobs/*` 页面，以纯合成简历走完整闭环：

1. 粘贴文本并确认事实、`不限城市` 和两条原子经历证据；原文确认后立即删除。
2. 生成 31 个当前岗位的三组推荐；目标美团岗位显示资格 4/4 已核对、证据 1/5，并保留 1 个待确认要求。
3. 进入岗位详情，当前匹配不会因加载顺序被误删。
4. 生成逐段优化稿；项目区块得到真实改写及 3 条岗位原句引用，教育和校园区块原序保留。
5. 接受项目建议后，最终完整预览只替换该区块；页面生成 9,125 字节 DOCX。
6. 通过数据控制删除本轮匿名 owner 的全部合成个人数据，页面返回“删除完成”。

响应式测量：

| 页面 | 1280 px | 320 px |
|---|---|---|
| 岗位目录 | `1265 = 1265`，无全局横向溢出 | `305 = 305` |
| 目标岗位详情 | 正确显示在读、4 天、3 个月 | `305 = 305` |
| 31 条推荐分组 | 31 张卡片，`1265 = 1265` | `305 = 305` |
| 逐段优化与最终预览 | 编辑框自动增高，`1265 = 1265` | 编辑框 `clientHeight = scrollHeight = 149`，页面 `305 = 305` |

表中等式为 `clientWidth = scrollWidth`。

## 6. Gate 边界

- G2：重新通过。
- G0：0/2，尚未启动；只有 coco 明确启动后才进行参与者校准。
- G1：未开始。
- G3：0/3；三个来源继续为 `pending_review`，公开 `/v1/jobs` 保持为空。
- 产品证据：仍为 `E0`；本记录不能用于支持 H-PROBLEM-001 或 H-VALUE-001。
- 公开 AI：继续关闭，仍需供应商、隐私、合规和增量价值 Gate。
