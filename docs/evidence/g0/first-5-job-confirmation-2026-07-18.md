# coco 对首批 5 条研究样本的确认

## 确认事实

| 项目 | 记录 |
|---|---|
| 确认时间 | `2026-07-18T13:01:09.669+08:00` |
| 确认人 | `coco` |
| 确认触发 | 在当前 Codex 任务被询问“首批 5 条候选中哪些可以进入研究样本”后，coco 明确回复“都可以” |
| 确认范围 | 允许下列 5 条岗位进入仅限本地 `dev/test` 的 G0 研究样本 |
| 人工岗位进度影响 | 从 `0/20-30` 更新为 `5/20-30` |

确认进入研究样本的岗位为：

1. `G0-CAND-001`：游戏用户研究
2. `G0-CAND-002`：业务管理运营
3. `G0-CAND-003`：产品经理(技术背景)
4. `G0-CAND-004`：产品运营
5. `G0-CAND-005`：产品策划

对应的结构化记录见[首批 5 条岗位候选表](first-5-job-review-candidates.csv)。`manual_review_result=confirmed` 只表达 coco 已作出本地研究样本纳入决定；自动提取中保留的 `unknown`、`conflict` 和原始来源证据没有因此被改写。

确认后这 5 行已转为 `import_mode=manual`，同时保留 `source_key`、`source_job_id`、首次发现时间和最后采集核验时间，避免把人工纳入决定误写成纯手工发现。

## 不受本次确认影响的边界

- 腾讯来源仍为 `pending_review`，本次确认不是来源访问政策审批、公开发布许可或“绝对真实”背书。
- 公开 `/v1/jobs` 仍应返回空列表；研究路由仍只在 `dev/test` 注册，不得因此进入生产目录。
- 自动化代表来源仍为 `0/1`，因为访问政策和连续运行稳定性尚未通过。
- 工程切片高风险金标仍为 `0/12-20`；这 5 条人工岗位样本不是匹配结论金标。
- 当前仍为 **Phase 0 / E0**。这不是用户访谈、协议校准、礼宾实验、72 小时行为或用户价值证据，不改变 G0/G1 状态。
- 协议校准、正式实验和 72 小时回访分别保持 `0/2`、`0/6` 和 `0/6`。

如果后续官方页面失效、活动状态变化或出现新的主体/字段冲突，应保留本确认记录，并对相应样本另记复核结果，不得静默覆盖历史确认。
