# MVP 路线与当前决策面板

> 本文是阶段、证据、Gate 和下一决定的唯一动态事实源。工程完成、岗位数量和页面可用性不等于用户价值证据。

## 1. 当前快照

| 项目 | 当前值 |
|---|---|
| 更新日期 | 2026-07-19 |
| 当前阶段 | Build：`G2 Local Complete MVP` 工程候选收口 |
| 当前切片 | 真实 OpenAI-compatible 接口冒烟与 G2 Gate 复核 |
| 当前实现契约 | [PRD v0.2：本地完整 MVP](01-prd-v0.2.md) |
| 当前产品证据 | `E0`：没有可复核目标用户行为证据；H-PROBLEM-001、H-VALUE-001 均未判定 |
| 本地岗位目录 | 31 条：腾讯 14、南开就业网·好未来 7、美团 10 |
| 来源政策 | 三个来源均为 `pending_review`，只进入 `local_mvp`；公开 `/v1/jobs` 为 0 |
| 金标 | 32 个命名工程金标通过；不计为用户研究样本 |
| 工程质量 | 237 项测试、全工作区类型检查、生产构建、196 文件 lint 和生产依赖审计通过 |
| AI | 受控适配器、模拟接口和模板降级通过；真实供应商冒烟未执行，公开环境关闭 |
| 参与者验证 | 暂停；G2 完成后先做 2 人校准，再做 6 人任务和 72 小时回访 |
| 下一决定 | 使用本机密钥完成一次去标识化合成数据冒烟，通过后评审 G2 |
| 下一决定日期 | 2026-07-19 |

工程证据见 [G2 本地完整 MVP 工程验收记录](evidence/g2/local-complete-mvp-engineering-2026-07-18.md)。

## 2. 当前最高产品风险

| ID | 假设 | 当前证据 | 验证时机 |
|---|---|---|---|
| H-PROBLEM-001 | 目标学生仍需要跨官方来源的资格与经历证据决策层 | E0，未验证 | G2 后的 2 人校准与 6 人任务 |
| H-VALUE-001 | 来源/时效、三轴推荐和简历对照修改能改善投递决定且不造成错误劝退 | E0，未验证 | G2 后的正式任务与 72 小时回访 |

当前接受先投入完整 MVP 工程成本再验证价值的风险。岗位数量、抓取成功、AI 调用、DOCX 或自动化测试都不能支持这两个假设。

## 3. Gate 状态

固定顺序：

```text
G2 Local Complete MVP
  -> G0 2 人协议校准
  -> G1 6 人 MVP 用户价值验证

G3 数据可持续与 G2 并行
G0 + G1 + G2 + G3 -> G4 Private Alpha
```

| Gate | 通过条件摘要 | 当前状态 | 当前决定 |
|---|---|---|---|
| G2 Local Complete MVP | 3 来源 30–100 岗位、三种简历输入、三轴、推荐、受控 AI/模板、DOCX、官方链接、五态决定和删除闭环 | 进行中 | 工程闭环候选已形成；真实 AI 冒烟后评审，不提前标记通过 |
| G0 协议可用 | 完整 MVP 做 2 人校准，术语、任务、记录和外链返回无需额外教学 | 暂停 | G2 后开始；历史 0/2 |
| G1 MVP 用户价值 | 4/6 找到 3 个岗位；5/6 正确区分四类概念；3/6 完成高质量决定且 2 人自报投递；守护指标为 0 | 未开始 | G0 后开始 |
| G3 数据可持续 | 3 个来源通过准入并可独立失败；连续运行、新鲜度、追溯和官方链接达标 | 进行中，0/3 | 三来源技术可读，但均未获准公开 |
| G4 Private Alpha | G0–G3 全部通过，金标扩至 30，删除/恢复/运行演练完成 | 未开始 | 不扩大邀请 |
| G5 Beta | Alpha 失败模式可控，金标扩至 60，公开合规与安全门通过 | 未开始 | 不公开扩张 |

任何硬条件漏检、错误劝退、虚构经历、未确认事实参与结论、跨 owner 越权或隐私事件都不能用平均指标抵消。

## 4. G2 切片状态

### S0：契约与基线

- [x] PRD v0.2 与 ADR-0011/0012/0013 固定“先完整 MVP、后参与者验证”。
- [x] 历史 `/research/*` 保留；正式产品使用 `/jobs/*`。
- [x] 研究筛选、移动抽屉、键盘语义和相关测试收口。
- [x] README、路线、架构、安全、体验、匹配和 API 契约一致。

### S1：三来源岗位目录

- [x] PostgreSQL 提供正式 `/v1/jobs`、详情、facets、覆盖率和已知/未知条件语义。
- [x] 腾讯、南开就业网·好未来、美团三个适配器各有离线夹具和受限网络策略。
- [x] 本地目录 31 条，三个来源各至少 5 条；来源内幂等、不可变版本和要求集成立。
- [x] 跨来源疑似重复只生成复核项；未说明字段保持 `unknown`。
- [x] `pending_review` 只允许 `local_mvp`，公开目录保持空。

### S2：匿名 owner、简历与数据控制

- [x] localhost 匿名 owner、HttpOnly 会话、Origin/CSRF 和 owner 级鉴权。
- [x] PDF、DOCX、文本输入；5 MiB、MIME/魔数、宏、加密、ZIP bomb 和扫描 PDF 失败路径。
- [x] `ProfileFact`、`JobPreference`、`ResumeEvidence` 分组确认与不可变修订。
- [x] 原文件/原文确认后立即删除且最长 24 小时；数据库只保存无正文候选元数据，授权读取时才从加密临时文本即时重建。
- [x] 结构化 owner 数据最长 30 天自动删除；DOCX 密文最长 24 小时，无正文审计和删除墓碑最长 90 天。
- [x] 删除墓碑、epoch、并发到期清理、迟到任务拒绝、状态查询和删除后不可恢复访问。

### S3：三轴匹配与推荐

- [x] `JobRequirementSet`、32 个命名金标和毕业年份/中文月份边界。
- [x] 不可变 `MatchRun` 与资格、经历证据、偏好三轴。
- [x] `RecommendationRun` 冻结候选、要求集、画像修订、规则版本与来源核验时间。
- [x] 排序使用资格、偏好、证据、新鲜度和稳定 ID；不展示数字分数。
- [x] 推荐原因、未知项、来源、官方链接和五态 `JobDecision` 可用。

### S4：受控 AI、模板与 DOCX

- [x] OpenAI-compatible HTTPS 适配层、可替换后端配置来源、超时、响应大小、无工具调用和 Schema 校验。
- [x] 模型只能选择已确认 evidence/requirement ID；最终文本由服务端确定性组装。
- [x] 用户可逐段接受、拒绝或编辑；AI 不自动修改最终简历。
- [x] AI 不可用时模板降级；复制和冻结后的 ATS DOCX 可用。
- [x] 模拟供应商、安全失败和事实引用测试通过。
- [ ] coco 通过 `pnpm ai:configure` 填写本地后端配置后完成一次真实兼容接口冒烟。

### S5：运行与端到端

- [x] `web-api`、采集 CLI/worker、`match-worker` 和 PostgreSQL 任务表保持权限边界。
- [x] 采集任务使用 `source_id`，用户任务使用 `owner_id + owner_epoch`，约束禁止混用。
- [x] 推荐、优化、DOCX、决定、删除和删除后拒绝旧任务的浏览器闭环通过。
- [x] 320 px 下目录、详情、推荐、优化和数据控制无全局横向溢出；主要操作目标达标。
- [x] 全量安全、数据库、幂等、构建、类型和 lint 工程门通过。

G2 只有在真实 AI 冒烟完成并复核以上证据后才能标记完成。真实微信内浏览器真机观察放入 G0 的 2 人校准，不得以桌面浏览器截图冒充用户证据。

## 5. 数据来源进度

| 来源 | 本地岗位 | 当前事实 | G2 | G3 |
|---|---:|---|---:|---:|
| 腾讯官方招聘 `p_104` | 14 | 官方企业域名公开接口；聚合占位 ID 被拒绝；政策待审批 | 14 | 0 |
| 南开就业网·好未来 2027 暑期实习 | 7 | 高校官方单页列出企业与 Moka 申请链接；岗位级职责未知 | 7 | 0 |
| 美团官方招聘产品实习 | 10 | 企业自有域名公开列表/详情接口；字段未说明时保持未知 | 10 | 0 |

三个来源的 `accessPolicyAccepted` 均未通过，因此只能由维护者显式低频探测并用于本机目录。不得通过登录、验证码、代理 IP、浏览器绕过或放宽白名单满足数量。

## 6. 下一行动

### 下一小时

1. coco 在本机运行 `pnpm ai:configure` 填写接口地址、模型和密钥；配置只进入 Git 忽略的后端本地文件，不进入聊天、数据库或 Git。
2. 运行 `pnpm ai:smoke`，使用合成、去标识化、已确认证据做一次真实兼容接口冒烟，并检查无新增事实、无工具调用和模板降级。
3. 冒烟通过后更新本面板并评审 G2；失败则保留模板、记录供应商错误，不影响岗位推荐链路。

### G2 之后

1. 固定构建、岗位目录与术语，做 2 人协议校准；校准样本不计入 6 人正式任务。
2. 受影响内容修改后重新校准，再做 6 人任务与 72 小时回访。
3. 公开 AI 仍需至少 4/6 在理解正确率或决策效率上优于模板且无新增事实错误。

## 7. 不可改变边界

- 只抓允许技术访问的官方公开来源；不抓综合平台，不绕过登录、验证码和访问控制。
- 技术可访问不等于获准公开；`pending_review` 只能用于本机 `local_mvp`。
- 岗位未说明字段保持 `unknown`；资格、证据、偏好分开；无匹配百分比和自动劝退。
- AI 只优化表达，不能修改三轴、创造经历、调用工具或接收原文件。
- 用户在官方页面自行投递；不自动填写、模拟登录或批量投递。
- PostgreSQL 是唯一查询和任务真源；不引入 Redis、向量库、独立搜索、消息总线、生产 Playwright 或公共管理后台。
- 不做 OCR、旧版 `.doc`、宏、加密文档或复杂版式复刻。
- 原文件/原文最长 24 小时，结构化 owner 数据最长 30 天，用户可提前删除。

## 8. 面板更新规则

每次更新必须写清新增证据、受影响切片、Gate 变化和下一决定。没有目标用户证据时只能写工程完成，不得写“价值已验证”“成熟”或“受到用户认可”。
